package it.petrillo.jbomberman.util;

/**
 * The Direction enum represents cardinal directions in a two-dimensional grid,
 * such as up, down, left, and right.
 */
public enum Direction {
    UP, DOWN, LEFT, RIGHT
}
